language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t ehermstad/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true ehermstad/docker-react npm run test
